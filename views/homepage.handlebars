
  {{#each posts as |post|}}
  <div class="card  my-2">
    <div class="card-header ">
     <h5><span class="float-let"> <a href="#" class="card-link">{{post.title}}</a></span>
       <span class = "float-right fs-5">Posted by {{post.user.user_name}} on {{format_date post.date_created}} </span></h5>
    </div>
    <div class="card-body rounded">
      
      <p class="card-text">{{post.description}}</p>
    
    </div>
    
  </div>
  {{/each}}
